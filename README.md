# encoder-decoder-transformer
russian-to-tatar translation model (Attention is all you need implementation) (pytorch + huggingface tokenizer)
couldn't really train it properly, had to reduce model complexity, but surprisingly there is still some comprehension noticable
